apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grafana
  namespace: grafana-operator
spec:
  to:
    kind: Service
    name: grafana-service
  port:
    targetPort: 3000
  tls:
    termination: edge
