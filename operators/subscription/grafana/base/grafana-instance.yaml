apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  labels:
    dashboards: "grafana"
  namespace: grafana-operator
spec:
  config:
    log:
      mode: console
    auth.anonymous:
      enabled: "true"
      org_role: Admin
